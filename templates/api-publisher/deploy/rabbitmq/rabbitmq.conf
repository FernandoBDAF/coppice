# Load the topology (exchanges/queues/bindings) at boot so the API can verify
# it passively and never declares anything itself. The management plugin (and
# its HTTP API on 15672) ships in the rabbitmq:*-management image.
load_definitions = /etc/rabbitmq/definitions.json
