apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k6-results
  namespace: microservice
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
