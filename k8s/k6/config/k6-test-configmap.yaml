apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-test-scripts
data:
  # This is a template ConfigMap. Each test will have its own ConfigMap
  # that references the appropriate script from the scripts directory.
  # Example:
  # profile-api-basic-load.js: |
  #   import http from "k6/http";
  #   ...
